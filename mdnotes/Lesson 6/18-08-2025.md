# Language Models

I think: seems to describe word embeddings as something trained within a language model but extractable in their own right.

So you could train a language model with an embedding layer and then reuse the layer elsewhere with the same weights.

Is that 'transfer learning'?